# Production Environment Variables for Azure VM
# ⚠️ Copy this file to .env and fill in the values
# ⚠️ NEVER commit .env to git!

# PostgreSQL (used by compose.prod.yaml)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE

# JWT (generate secret: openssl rand -hex 64)
JWT_SECRET=CHANGE_ME_GENERATE_SECURE_SECRET_256_BITS

# Optional: JWT expiration times (milliseconds)
# JWT_EXPIRATION=86400000
# JWT_REFRESH_EXPIRATION=604800000
