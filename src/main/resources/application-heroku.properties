# Heroku: disable devtools and Docker Compose
spring.devtools.restart.enabled=false
spring.docker.compose.skip=true

# Heroku Redis sets REDIS_URL (redis://...) – parsed in HerokuRedisConfig when set
# If REDIS_URL is not set, cache will fail; add Heroku Redis add-on or set REDIS_HOST/REDIS_PORT

# Quieter logging in production
logging.level.org.hibernate.SQL=WARN
logging.level.org.springframework.security=WARN
logging.level.org.springframework.web=INFO
