# Single stack: app + Postgres + Redis. No external DB URL config—everything uses services below.
# Run: docker compose up -d --build
# API: http://localhost:8081

services:
  app:
    build: .
    image: djaj-bladi-app:latest
    container_name: djaj-bladi-app
    environment:
      # DB from postgres service (same compose)—no URL config needed
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/djaj_bladi
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: postgres
      # Redis from redis service (same compose); use default profile (Redis enabled)
      SPRING_PROFILES_ACTIVE: default
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      PORT: "8081"
      SPRING_DOCKER_COMPOSE_SKIP: "true"
      SPRING_DEVTOOLS_RESTART_ENABLED: "false"
      JWT_SECRET: ${JWT_SECRET:-dev-jwt-secret-change-in-production}
    ports:
      - "8081:8081"
    networks:
      - djaj-bladi-network
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  postgres:
    image: postgres:16-alpine
    container_name: djaj-bladi-postgres
    environment:
      POSTGRES_DB: djaj_bladi
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - djaj-bladi-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: djaj-bladi-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - djaj-bladi-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    command: redis-server --appendonly yes
    restart: unless-stopped

  pgadmin:
    profiles:
      - admin
    image: dpage/pgadmin4:latest
    container_name: djaj-bladi-pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_EMAIL:-admin@example.com}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_PASSWORD:-admin}
      PGADMIN_CONFIG_SERVER_MODE: "False"
    ports:
      - "5050:80"
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    networks:
      - djaj-bladi-network
    depends_on:
      - postgres

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
  pgadmin_data:
    driver: local

networks:
  djaj-bladi-network:
    driver: bridge
